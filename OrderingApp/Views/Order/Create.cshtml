@*@{
        ViewData["Title"] = "Create";
    }

    <h2>Create</h2>*@
@using OrderingApp.ViewModel;
@model CreateOrderViewModel


@{
    ViewData["Title"] = "Create";
}
<h2>Create</h2>
<h4>Order</h4>
<hr />


<div class="row">
    <div class="col-md-12">
        <form asp-action="Create">
            <input type="hidden" asp-for="OrderId" value="@Model.OrderId" />
            @*<input type="hidden" asp-for="ProductList" value="@Model.ProductList" />
            <input type="hidden" asp-for="CustomerList" value="@Model.CustomerList" />*@
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="col-sm-12">
                <div class="col-sm-6">
                    <div class="table-responsive px-2 pb-2">
                        <table id="orders-list" class="table align-items-center table-flush table-striped text-center w-100">
                            <thead class="thead-light"></thead>
                            <thead>
                                <tr>
                                    <th>Items</th>
                                    <th>Price</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var selectedItem in Model.SelectedItems)
                                {
                                    <tr>
                                        <td>@selectedItem.Name</td>
                                        <td>@selectedItem.Price</td>
                                    </tr>
                                }
                                <tr>
                                    <td>Total</td>
                                    <td>@Model.TotalAmount</td>
                                </tr>
                                <tr>
                                    <td>Discount </td>
                                    <td>@Model.DiscountAmount</td>
                                </tr>
                                <tr>
                                    <td>Grand Total</td>
                                    <td>@Model.GrandTotalAmount</td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="col-sm-6">
                    <div class="form-group">
                        <label asp-for="SelectedCustomerId" class="control-label">Customer</label>
                        @*<select asp-for="SelectedCustomer" class="form-control">
                                <option value="">-- Select Customer --</option>
                                @foreach (var emp in Model.Customers)
                                {
                                    <option value="@emp">@emp.Name</option>

                                }
                            </select>*@
                        <select asp-for="SelectedCustomerId" class="form-control" asp-items="@Model.Customers">
                            <option>Select one</option>
                        </select>
                        <span asp-validation-for="SelectedCustomerId" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="SelectedItemId" class="control-label">Item</label>
                        @*<select asp-for="SelectedItem" class="form-control">
                                <option value="">-- Select Item --</option>
                                @foreach (var item in Model.Items)
                                {
                                    <option value="@item">@item.Name</option>

                                }
                            </select>*@
                        <select asp-for="SelectedItemId" class="form-control" asp-items="@Model.Items">
                            <option>Select one</option>
                        </select>
                        <span asp-validation-for="SelectedItemId" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <input type="submit" name="add" value="Add"
                               formaction="AddToOrder" formmethod="post" />
                        @*<input type="submit" value="Add" class="btn btn-default" />*@
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Remove" class="btn btn-default" />
                    </div>
                </div>
            </div>


            @*<div class="form-group">
                    <label asp-for="Department" class="control-label"></label>
                    <input asp-for="Department" class="form-control" />
                    <span asp-validation-for="Department" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="City" class="control-label"></label>
                    <input asp-for="City" class="form-control" />
                    <span asp-validation-for="City" class="text-danger"></span>
                </div>*@
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>
